eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      default-zone: http://${eureka.instance.hostname}/${eureka-server.port}/eureka/

eureka-server:
  port: 8761

spring:
  cloud:
    config:
      override.system-properties: false